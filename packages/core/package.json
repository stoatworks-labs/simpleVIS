{
  "name": "@simplevis/core",
  "version": "0.1.0",
  "description": "Isomorphic MVR/GDTF parsing and DMX evaluation. No DOM, no Node APIs — runs in a browser tab, in Tauri's webview, and under vitest.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "fast-xml-parser": "^4.5.1",
    "fflate": "^0.8.2"
  },
  "devDependencies": {
    "@types/node": "^26.1.2"
  }
}
