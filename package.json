{
  "name": "simplevis",
  "private": true,
  "version": "0.4.0",
  "description": "Lightweight real-time lighting visualiser — MVR import, Art-Net/sACN/DMX input, volumetric beams",
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
